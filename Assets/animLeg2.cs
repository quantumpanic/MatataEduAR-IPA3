using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class animLeg2 : MonoBehaviour {

	// Use this for initialization
	void Start () {
        GetComponent<Animator>().Play(0, 0, 0.5f);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
